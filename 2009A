#include <stdio.h>
 
int main() {
    int t;
    if (scanf("%d", &t) != 1) return 0;
 
    while (t--) {
        int a, b;
        scanf("%d %d", &a, &b);
        printf("%d\n", b - a);
    }
 
    return 0;
}
